import React from 'react';
import GLBViewer from './GLBViewer';

const App: React.FC = () => {
    return (
        <div>
            <h1>Drag and Drop GLB Files</h1>
            <GLBViewer />
        </div>
    );
};

export default App;
